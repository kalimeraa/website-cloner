# Website Cloner Design

Website Cloner is a standalone Electron desktop archiver for Windows, macOS, and Linux. It has no Docker, Redis, or MongoDB runtime dependency; jobs and manifests are stored locally. It supports HTTP(S), SOCKS4, and SOCKS5 proxies with optional credentials, saves source files and screenshots to a user-selected directory, and reports challenge pages without bypassing them.
